{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(Windows) ${fileBasenameNoExtension}",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/${fileBasenameNoExtension}.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${fileDirname}",
            "environment": [],
            "console": "integratedTerminal"
        }
    ]
}